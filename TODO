Make the control changeable (isntead of checking the key pressed, make a button that is assigned to a certain action)
add sound and music ✓
add menu
add options menu (resolution, max fps, music and sfx volume)
add other gamemodes
add max score
